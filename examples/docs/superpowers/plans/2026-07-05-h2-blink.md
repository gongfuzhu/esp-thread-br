# H2 Blink 功能实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `ot_iot_device`(ESP32-H2)新增 `cmd:"blink"` 命令——收到后立即点亮板载 LED,固定时长后自动熄灭,并按现有链路上报。

**Architecture:** 在 CoAP `/ctrl` 命令处理里新增 blink 分支。熄灭动作交给一个**独立的 FreeRTOS 单次定时器** `s_blink_timer` 承载(绝不在 CoAP 回调里 `vTaskDelay`,避免阻塞 OpenThread 任务),与现有承载"抖动上报"的 `s_report_timer` 职责分离。上报固定 `state:"off"` + `action:"blink"`,不读竞态的 GPIO 瞬时值。BR 侧与 `on/off/query` 契约零改动。

**Tech Stack:** ESP-IDF v6.0.2、OpenThread(CoAP)、FreeRTOS timers、cJSON(托管组件 `espressif__cjson`)、Kconfig。

## Global Constraints

- 目标芯片 ESP32-H2;编译验证 `idf.py build` **必须**在 ESP-IDF PowerShell/CMD 执行,**不能**在 Git Bash(报 "MSys/Mingw is no longer supported")——由用户手动执行并回贴结果。
- **无主机端测试套件**;验证 = 编译通过 + 设备端 monitor 观察。
- **不手动改 `sdkconfig`**(自动生成);配置项加在 `Kconfig.projbuild`,默认值即生效。
- `main` 组件**不写** REQUIRES/PRIV_REQUIRES(IDF MINIMAL_BUILD 下会挡掉隐式头)。
- 不在定时器/回调任务栈里放大局部数组或做 cJSON(定时器任务栈仅 ~2KB)。
- 定时器回调在定时器任务上下文,访问 OpenThread API **必须自行持 OT 锁**(`esp_openthread_lock_acquire/release`)。
- 不改动 BR 侧任何代码、MQTT topic 结构,或 `on/off/query` 语义。

---

## File Structure

- `ot_iot_device/main/Kconfig.projbuild` — 新增 `IOT_DEVICE_BLINK_MS` 配置项。
- `ot_iot_device/main/iot_device.c` — 全部逻辑改动:blink 定时器句柄与回调、`parse_command` 扩展、`device_report` 扩展、`ctrl_request_handler` blink 分支、`iot_device_start` 创建定时器。
- `device_switch.c/.h` — **不改动**(`device_switch_set(true/false)` 已足够)。

设计依据见 `openspec/changes/add-h2-blink/design.md`;需求见 `openspec/changes/add-h2-blink/specs/device-blink/spec.md`。

---

### Task 1: 新增 blink 时长配置项

**Files:**
- Modify: `ot_iot_device/main/Kconfig.projbuild`(在 `IOT_DEVICE_COAP_PORT` 之后、`endmenu` 之前插入)

**Interfaces:**
- Consumes: 无
- Produces: 编译期宏 `CONFIG_IOT_DEVICE_BLINK_MS`(int,默认 500),供 Task 4 使用。

- [ ] **Step 1: 在 Kconfig.projbuild 添加配置项**

在 `ot_iot_device/main/Kconfig.projbuild` 中,`config IOT_DEVICE_COAP_PORT` 块之后、`endmenu` 之前插入:

```kconfig
    config IOT_DEVICE_BLINK_MS
        int "Blink duration (ms)"
        default 500
        help
            收到 cmd:"blink" 后板载 LED 点亮的持续毫秒数，到期自动熄灭。
```

- [ ] **Step 2: 提示用户编译验证配置项被识别**

请用户在 ESP-IDF PowerShell/CMD、`ot_iot_device` 目录下执行:

```
idf.py build
```

Expected: 编译通过。`build/config/sdkconfig.h` 中出现 `#define CONFIG_IOT_DEVICE_BLINK_MS 500`。

- [ ] **Step 3: Commit**

```bash
git add ot_iot_device/main/Kconfig.projbuild
git commit -m "feat(ot_iot_device): add IOT_DEVICE_BLINK_MS config"
```

---

### Task 2: 添加 blink 定时器句柄与熄灭回调

**Files:**
- Modify: `ot_iot_device/main/iot_device.c`(全局区 line 33 附近;新增回调函数;`iot_device_start` line 246 附近)

**Interfaces:**
- Consumes: `device_switch_set(bool)`(来自 `device_switch.h`);`esp_openthread_lock_acquire/release`;现有 `s_report_timer` 的创建模式作参照。
- Produces: 全局 `static TimerHandle_t s_blink_timer`;函数 `static void blink_off_timer_cb(TimerHandle_t t)`,到期时持锁熄灭 LED。供 Task 4 启动。

- [ ] **Step 1: 声明 blink 定时器句柄**

在 `iot_device.c` line 33 `static TimerHandle_t s_report_timer = NULL;` 之后新增一行:

```c
static TimerHandle_t s_blink_timer = NULL;
```

- [ ] **Step 2: 实现熄灭回调**

在 `report_timer_cb`(line 137-142)函数之后新增。回调运行在定时器任务上下文,访问 OT 状态需持锁(与 `report_timer_cb` 同款持锁模式);`device_switch_set` 只操作 GPIO,持锁是为与 OT 任务对 switch 状态的读写串行化,保持一致性:

```c
// blink 熄灭定时器回调:到点关灯。定时器任务上下文，持锁与 OT 任务串行化状态访问。
static void blink_off_timer_cb(TimerHandle_t t) {
    (void)t;
    esp_openthread_lock_acquire(portMAX_DELAY);
    device_switch_set(false);
    esp_openthread_lock_release();
    ESP_LOGI(TAG, "blink: LED off");
}
```

- [ ] **Step 3: 在启动时创建定时器**

在 `iot_device_start`(line 246)`s_report_timer = xTimerCreate(...)` 之后新增一行。初始周期 1 tick(启动时会被 `xTimerChangePeriod` 覆盖),单次(`pdFALSE`):

```c
    s_blink_timer = xTimerCreate("iot_blink", 1, pdFALSE, NULL, blink_off_timer_cb);
```

- [ ] **Step 4: 提示用户编译验证**

请用户在 ESP-IDF PowerShell/CMD、`ot_iot_device` 目录下执行:

```
idf.py build
```

Expected: 编译通过,无未使用函数告警(`blink_off_timer_cb` 已被 `xTimerCreate` 引用)。

- [ ] **Step 5: Commit**

```bash
git add ot_iot_device/main/iot_device.c
git commit -m "feat(ot_iot_device): add blink-off timer and callback"
```

---

### Task 3: 扩展命令解析支持 blink

**Files:**
- Modify: `ot_iot_device/main/iot_device.c`(`parse_command` line 164-192,及其调用点 line 202)

**Interfaces:**
- Consumes: cJSON API;现有 `parse_command` 签名。
- Produces: `parse_command` 新增出参 `bool *is_blink_out`。新签名:
  `static bool parse_command(const char *json, char *reqid_out, size_t reqid_cap, bool *on_out, bool *is_query_out, bool *is_blink_out)`。Task 4 依赖此出参。

- [ ] **Step 1: 修改 parse_command 签名与实现**

将 `parse_command`(line 164-192)整体替换为下面版本——新增 `is_blink_out` 出参并初始化,新增 `blink` 命令分支:

```c
// 解析 {"reqid":"..","cmd":"on|off|query|blink"}。识别成功返回 true。
static bool parse_command(const char *json, char *reqid_out, size_t reqid_cap,
                          bool *on_out, bool *is_query_out, bool *is_blink_out) {
    cJSON *root = cJSON_Parse(json);
    if (root == NULL) {
        return false;
    }
    bool ok = false;
    reqid_out[0] = '\0';
    *is_query_out = false;
    *is_blink_out = false;
    *on_out = false;

    cJSON *reqid = cJSON_GetObjectItem(root, "reqid");
    if (cJSON_IsString(reqid) && reqid->valuestring != NULL) {
        strncpy(reqid_out, reqid->valuestring, reqid_cap - 1);
        reqid_out[reqid_cap - 1] = '\0';
    }
    cJSON *cmd = cJSON_GetObjectItem(root, "cmd");
    if (cJSON_IsString(cmd) && cmd->valuestring != NULL) {
        if (strcmp(cmd->valuestring, "on") == 0) {
            *on_out = true; ok = true;
        } else if (strcmp(cmd->valuestring, "off") == 0) {
            *on_out = false; ok = true;
        } else if (strcmp(cmd->valuestring, "query") == 0) {
            *is_query_out = true; ok = true;
        } else if (strcmp(cmd->valuestring, "blink") == 0) {
            *is_blink_out = true; ok = true;
        }
    }
    cJSON_Delete(root);
    return ok;
}
```

- [ ] **Step 2: 更新调用点声明局部变量**

在 `ctrl_request_handler`(line 200-202)中,将:

```c
    char reqid[REQID_MAX];
    bool on = false, is_query = false;
    if (!parse_command(payload, reqid, sizeof(reqid), &on, &is_query)) {
```

替换为:

```c
    char reqid[REQID_MAX];
    bool on = false, is_query = false, is_blink = false;
    if (!parse_command(payload, reqid, sizeof(reqid), &on, &is_query, &is_blink)) {
```

- [ ] **Step 3: 提示用户编译验证**

请用户在 ESP-IDF PowerShell/CMD、`ot_iot_device` 目录下执行:

```
idf.py build
```

Expected: 编译通过。此时 `is_blink` 已声明但尚未使用于分支逻辑(Task 4 会用),GCC 对已赋值的局部 bool 通常不报未使用告警;若报,Task 4 会消除。

- [ ] **Step 4: Commit**

```bash
git add ot_iot_device/main/iot_device.c
git commit -m "feat(ot_iot_device): parse blink command"
```

---

### Task 4: 实现 blink 执行分支与上报

**Files:**
- Modify: `ot_iot_device/main/iot_device.c`(`device_report` line 88-133;`ctrl_request_handler` line 195-230)

**Interfaces:**
- Consumes: Task 2 的 `s_blink_timer`;Task 3 的 `is_blink`;`CONFIG_IOT_DEVICE_BLINK_MS`(Task 1);现有 `s_report_timer`、`device_switch_set`、`device_report`。
- Produces: `device_report` 新增 `const char *action` 参数。新签名:
  `static void device_report(const char *reqid, const char *action)`。blink 场景固定上报 `state:"off"` + `action:"blink"`。

- [ ] **Step 1: 扩展 device_report 支持 action 与固定 off**

将 `device_report`(line 88-133)整体替换。新增 `action` 参数:非 NULL 时加 `"action"` 字段;当 `action != NULL`(即 blink)时 `state` 固定为 `"off"`,不读 GPIO 瞬时值(避免与 blink-off 定时器竞态):

```c
// 构造状态 JSON 并单播上报到 BR(SRP server)的 ack 资源。
// action 非 NULL 时附加 "action" 字段，且 state 固定为 "off"（用于 blink，避免竞态读 GPIO）。
// 调用者必须已持 OT 锁。
static void device_report(const char *reqid, const char *action) {
    otInstance *inst = esp_openthread_get_instance();

    const otSockAddr *server = otSrpClientGetServerAddress(inst);
    if (server == NULL) {
        ESP_LOGW(TAG, "report: no SRP server address yet");
        return;
    }

    cJSON *root = cJSON_CreateObject();
    if (root == NULL) {
        return;
    }
    cJSON_AddStringToObject(root, "id", s_eui64_str);
    cJSON_AddStringToObject(root, "reqid", reqid);
    const char *state = action != NULL ? "off" : (device_switch_get() ? "on" : "off");
    cJSON_AddStringToObject(root, "state", state);
    if (action != NULL) {
        cJSON_AddStringToObject(root, "action", action);
    }
    char *json = cJSON_PrintUnformatted(root);
    cJSON_Delete(root);
    if (json == NULL) {
        return;
    }

    otMessage *msg = otCoapNewMessage(inst, NULL);
    if (msg == NULL) {
        free(json);
        return;
    }
    otCoapMessageInit(msg, OT_COAP_TYPE_NON_CONFIRMABLE, OT_COAP_CODE_POST);
    otCoapMessageAppendUriPathOptions(msg, CONFIG_IOT_DEVICE_ACK_URI);
    otCoapMessageSetPayloadMarker(msg);
    otMessageAppend(msg, json, (uint16_t)strlen(json));

    otMessageInfo info;
    memset(&info, 0, sizeof(info));
    info.mPeerAddr = server->mAddress;
    info.mPeerPort = CONFIG_IOT_DEVICE_COAP_PORT;

    otError err = otCoapSendRequest(inst, msg, &info, NULL, NULL);
    if (err != OT_ERROR_NONE) {
        otMessageFree(msg);
        ESP_LOGE(TAG, "report send err=%d", err);
    } else {
        ESP_LOGI(TAG, "reported state=%s%s%s reqid=%s", state,
                 action != NULL ? " action=" : "", action != NULL ? action : "", reqid);
    }
    free(json);
}
```

- [ ] **Step 2: 更新 report_timer_cb 传 action**

`report_timer_cb` 现在需要知道本次上报是否为 blink。新增一个全局标志,与 `s_pending_reqid` 并列(单槽,命令低频)。在 line 32 `static char s_pending_reqid[REQID_MAX];` 之后新增:

```c
static bool s_pending_is_blink = false;
```

将 `report_timer_cb`(line 137-142)替换为:

```c
static void report_timer_cb(TimerHandle_t t) {
    (void)t;
    esp_openthread_lock_acquire(portMAX_DELAY);
    device_report(s_pending_reqid, s_pending_is_blink ? "blink" : NULL);
    esp_openthread_lock_release();
}
```

- [ ] **Step 3: 在 ctrl_request_handler 加 blink 分支并设置上报标志**

将 `ctrl_request_handler` 中的命令执行段(line 206-219,从 `if (!is_query) {` 到 `s_pending_reqid[...] = '\0';`)替换为下面版本。blink 立即点亮并排定熄灭定时器;设置 `s_pending_is_blink` 供上报用:

```c
    if (is_blink) {
        device_switch_set(true);
        TickType_t blink_ticks = pdMS_TO_TICKS(CONFIG_IOT_DEVICE_BLINK_MS);
        if (blink_ticks < 1) {
            blink_ticks = 1;
        }
        if (s_blink_timer != NULL) {
            xTimerChangePeriod(s_blink_timer, blink_ticks, 0);
            xTimerStart(s_blink_timer, 0);
        }
        ESP_LOGI(TAG, "ctrl: blink %u ms reqid=%s", (unsigned)CONFIG_IOT_DEVICE_BLINK_MS, reqid);
    } else if (!is_query) {
        device_switch_set(on);
        ESP_LOGI(TAG, "ctrl: set switch=%d reqid=%s", on, reqid);
    } else {
        ESP_LOGI(TAG, "ctrl: query reqid=%s", reqid);
    }
    s_pending_is_blink = is_blink;

    // 组播命令(本地收包地址为多播，IPv6 多播首字节 0xff)加随机抖动，避免响应风暴。
    // 上报统一交给单次定时器承载：既实现抖动，又避免在 OT 回调里 vTaskDelay 阻塞协议栈。
    bool via_multicast = (info->mSockAddr.mFields.m8[0] == 0xff);
    uint32_t jitter_ms = via_multicast ? (esp_random() % 501) : 0;

    strncpy(s_pending_reqid, reqid, sizeof(s_pending_reqid) - 1);
    s_pending_reqid[sizeof(s_pending_reqid) - 1] = '\0';
```

注意:上面替换段的结尾(`strncpy(s_pending_reqid...)` 及其后 `s_pending_reqid[...] = '\0';`)与原代码衔接;原代码 line 221 起的 `if (s_report_timer != NULL) { ... }` 定时器启动块保持不变。

- [ ] **Step 4: 提示用户编译验证**

请用户在 ESP-IDF PowerShell/CMD、`ot_iot_device` 目录下执行:

```
idf.py build
```

Expected: 编译通过,无告警。所有 `is_blink`、`s_blink_timer`、`s_pending_is_blink`、`CONFIG_IOT_DEVICE_BLINK_MS` 均已被使用。

- [ ] **Step 5: Commit**

```bash
git add ot_iot_device/main/iot_device.c
git commit -m "feat(ot_iot_device): execute blink and report action"
```

---

### Task 5: 硬件联调验证

**Files:** 无(纯设备端验证)

**Interfaces:**
- Consumes: Task 1-4 的完整固件;运行中的 BR(MQTT↔CoAP 桥)与 MQTT broker。

- [ ] **Step 1: 烧录并监视**

请用户在 ESP-IDF PowerShell/CMD、`ot_iot_device` 目录下执行(替换实际串口):

```
idf.py -p COM<N> flash monitor
```

Expected: 设备启动,日志出现 `CoAP started, resource 'ctrl' registered` 和 SRP 注册成功,`dev/registry` 含该 EUI64。

- [ ] **Step 2: 下发单播 blink 命令**

通过服务端/MQTT 向 `cmd/unicast/<eui64>` 发布:

```json
{"reqid":"blink-1","cmd":"blink"}
```

Expected(设备 monitor):依次出现 `ctrl: blink 500 ms reqid=blink-1`、约 500ms 后 `blink: LED off`、以及 `reported state=off action=blink reqid=blink-1`。肉眼观察:板载 LED 亮约 0.5 秒后自动熄灭。

- [ ] **Step 3: 验证上行回执**

Expected:MQTT `dev/response` 收到 `{"id":"<eui64>","reqid":"blink-1","state":"off","action":"blink"}`。

- [ ] **Step 4: 组播 blink 验证抖动**

向 `cmd/multicast` 发布 `{"reqid":"blink-mc","cmd":"blink"}`。

Expected:设备 LED 闪一次;monitor 出现 `multicast cmd, report jitter <N> ms`(N 在 0~500);`dev/response` 收到对应回执。

- [ ] **Step 5: 回归验证 on/off/query**

依次下发 `{"reqid":"r1","cmd":"on"}`、`{"reqid":"r2","cmd":"off"}`、`{"reqid":"r3","cmd":"query"}`。

Expected:`on` 使 LED 常亮并保持、`off` 常灭、`query` 仅上报当前状态;三者上报的 JSON **不含** `action` 字段(仅 blink 才有),行为与改动前一致。
